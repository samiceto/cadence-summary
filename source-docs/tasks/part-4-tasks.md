# Tasks — Part 4: Product Requirements (Cadence)

- **[~] T4.1 Accounting connectors + sync** — QuickBooks/Xero/Stripe adapters; bi-directional invoice/customer/payment sync. Priority: P0. Estimate: 5d. Dependencies: T5.4, T8.1. DoD: sync imports + reflects status; contract tests pass. → **Stripe done** (`StripeConnector` list/map/write-back), `@cadence/domain` `syncConnection()` (idempotent upsert + version guard), worker `sync` queue wired, Stripe mapping contract tests (4) + DB-gated sync tests (2). **Remaining:** QuickBooks + Xero adapters, payment sync + write-back end-to-end (with T7.2 token vault).
- **[x] T4.2 Late-payment risk scoring** — Per-invoice risk/timing score. Priority: P0. Estimate: 2d. Dependencies: T6.1, T8.1. DoD: scores on open invoices; surfaced in UI. → `@cadence/domain` `scoring.ts` (`computeRiskScore` heuristic baseline `heuristic-v1` + `scoreOpenInvoices` batch), worker `scoring` queue, `GET /invoices` API surface. *(GBM model from T6.1 swaps in behind the same interface.)*
- **[x] T4.3 Sequence engine (email)** — Durable state-machine dunning sequences. Priority: P0. Estimate: 4d. Dependencies: T5.3, T6.2. DoD: resumable/idempotent steps; email sends logged. → `@cadence/domain` `sequences.ts` (`startSequence`/`advanceSequence`, idempotency key per step, payment closes sequence) + `drafting.ts` (template + cheap-LLM polish w/ fallback) + gated `send.ts`; worker `sequence`+`send` queues.
- **[x] T4.4 SMS + payment plans + pay link** — Add SMS channel, policy-bound plans, Stripe pay link. Priority: P0. Estimate: 3d. Dependencies: T4.3, T7.3. DoD: one-click pay works; plans within policy. → SMS via gated Twilio adapter; Stripe `hosted_invoice_url` pay link in drafts (migration 0002); policy-bound payment plans (`payment_plans` table migration 0004 + `negotiatePlan`/`proposePaymentPlan`, T6.4).
- **[x] T4.5 Cash application & reconciliation** — Match payments to invoices; idempotent write-back. Priority: P0. Estimate: 3d. Dependencies: T8.2, T8.3. DoD: no double-apply; balances reconcile. → `@cadence/domain` `payments.ts` `applyPayment` (idempotent, balance/status update, closes paid sequences, write-back) + `reconcileInvoices`. Validated live on Neon.
- **[x] T4.6 Policy/approval + blocklist** — Rules, tone limits, approvals, VIP/blocklist. Priority: P0. Estimate: 2d. Dependencies: T1.1, T7.3. DoD: no send to blocklist/VIP without approval. → policy engine (T1.1) + approval workflow (`GET /messages/pending-approval`, `POST /messages/:id/approve` → re-queue `approved`) + `POST /customers/:id/blocklist` (suppress all channels).
- **[x] T4.7 Dashboard** — DSO, aging, recovered cash, sequence performance. Priority: P1. Estimate: 2d. Dependencies: T12.1. DoD: metrics render from reconciled data. → `@cadence/metrics` `computeAging` + `GET /metrics/aging`; web `Aging` chart + north-star widget. Validated live on Neon ($13,400 aged correctly).
