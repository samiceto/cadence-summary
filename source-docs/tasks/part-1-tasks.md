# Tasks — Part 1: Vision & Mission (Cadence)

- **[x] T1.1 Policy-principles spec** — Encode relationship-safety + north-star as machine-enforceable rules. Priority: P0. Estimate: 1d. Dependencies: none. DoD: rules doc + schema referenced by policy engine. → `@cadence/security` `policy.ts`: `evaluateSendPolicy` (VIP/escalation approval, frequency cap, SMS quiet-hours defer) + `evaluateOutbound` chokepoint (consent+suppression+policy) enforced in every send.
- **[x] T1.2 North-star dashboard widget** — Recovered cash + DSO trend on primary dashboard. Priority: P0. Estimate: 1.5d. Dependencies: T12.1, T8.3. DoD: widget renders from reconciled data post-activation. → `apps/web` dashboard fetches `GET /metrics/north-star` + `/metrics/aging`; `NorthStar`/`Aging` components render reconciled data (null → insight placeholders for new tenants).
- **[x] T1.3 CI safety gate wiring** — Block builds failing relationship-safety evals. Priority: P0. Estimate: 0.5d. Dependencies: T10.3. DoD: failing tone eval fails CI. → `isToneSafe` gate (banned threat/shame/legal phrases) enforced in `draftMessage` + tone-safety evals in `drafting.test.ts` run by `pnpm test` in CI.
- **[x] T1.4 In-app mission statement** — Surface principles in onboarding/settings. Priority: P2. Estimate: 0.5d. Dependencies: none. DoD: visible, versioned copy. → `apps/web/app/mission/page.tsx` (versioned principles copy).
