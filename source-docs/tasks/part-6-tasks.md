# Tasks — Part 6: AI Strategy (Cadence)

- **T6.1 Feature pipeline + scoring model** — GBM/small model on payment history, batched nightly. Priority: P0. Estimate: 3d. Dependencies: T8.1, T5.3. DoD: risk scores produced + stored.
- **[~] T6.2 Message drafting (cheap model + templates)** — Tone/segment-personalized drafts. Priority: P0. Estimate: 2d. Dependencies: T5.4, T1.1. DoD: drafts pass tone-safety gate. → `@cadence/domain` `drafting.ts` (tone-bounded template + cheap-model polish, facts preserved). **Remaining:** automated tone-safety eval gate (T6.5).
- **[x] T6.3 Reply intent classifier** — Classify replies (PTP/dispute/STOP/question/other). Priority: P0. Estimate: 2d. Dependencies: T5.4. DoD: ≥90% on golden set; routes correctly. → `@cadence/domain` `replies.ts` `classifyReply` (deterministic STOP/dispute/PTP + cheap-LLM for ambiguous, safe fallback) + `handleReply` routing (suppress/pause). Validated live on Neon.
- **[x] T6.4 Negotiation reasoning (frontier)** — Policy-bound negotiation on live replies only. Priority: P1. Estimate: 2d. Dependencies: T6.3, T4.4. DoD: stays within policy; frontier only on negotiation. → `@cadence/domain` `negotiatePlan`: installment numbers computed by deterministic policy-clamped code; frontier model only phrases the (tone-safe) message. `POST /invoices/:id/plan`.
- **[~] T6.5 Eval suite + gates** — Tone-safety, intent, draft, negotiation evals in CI. Priority: P0. Estimate: 2d. Dependencies: T6.2, T6.3. DoD: CI blocks on threshold regression. → tone-safety eval (`isToneSafe` + template/LLM-rewrite gate tests) + reply-intent heuristic tests run in CI. **Remaining:** golden-set intent accuracy threshold + negotiation-policy evals.
- **[x] T6.6 Fallback/degradation** — Template fallback if LLM unavailable. Priority: P1. Estimate: 1d. Dependencies: T6.2. DoD: graceful degrade verified. → `draftMessage` + `classifyReply` degrade to template/heuristic on any LLM error; demonstrated live (OpenAI 401 → templates).
