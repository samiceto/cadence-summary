# Tasks — Part 3: Business Model (Cadence)

- **[x] T3.1 Stripe tiers + checkout** — 3 products/prices (Starter/Growth/Pro), annual default. Priority: P0. Estimate: 1.5d. Dependencies: T5.1. DoD: subscribe/upgrade works end-to-end. → `@cadence/billing` `checkout.ts` (`createCheckoutSession`) + `subscription.ts` (Stripe `customer.subscription.*` webhook → `subscriptions`/`tenants.tier`); `POST /billing/checkout`.
- **[x] T3.2 Tier limit enforcement** — Active-invoice/channel/multi-entity/API gates + upgrade prompts. Priority: P0. Estimate: 1.5d. Dependencies: T3.1, T4.1. DoD: over-limit soft-blocks with CTA. → `limits.ts` (`checkActiveInvoiceLimit`, `isChannelAllowed`) + `GET /billing/status` (usage vs limit). `TIER_LIMITS` gate SMS to Growth+.
- **[x] T3.3 Metered SMS billing** — Idempotent usage events for SMS bundles. Priority: P1. Estimate: 1d. Dependencies: T3.1, T4.4. DoD: usage metered + billed accurately. → idempotent `usage_events` at send + `summarizeSmsUsage` (bundle + overage) + `reportSmsUsageToStripe` (Stripe Billing Meters API, idempotent identifier). *(Needs a meter/price configured in Stripe.)*
- **[x] T3.4 Success-fee engine** — Compute opt-in fee on reconciled recoveries >90d + statement. Priority: P1. Estimate: 2d. Dependencies: T8.3, T7.4. DoD: auditable statement; charged correctly. → `success-fee.ts` `computeSuccessFee` (opt-in only, payments on >90d-overdue invoices, itemized statement) surfaced in `GET /billing/status`.
- **[x] T3.5 Refund/clawback handling** — Reverse success fee on refund/clawback. Priority: P2. Estimate: 1d. Dependencies: T3.4. DoD: fee reversed; audit recorded. → `@cadence/domain` `reversePayment` (idempotent negative payment, restores balance/status, `payment.refunded` audit); Stripe `charge.refunded` webhook wired. Success fee nets out automatically.
