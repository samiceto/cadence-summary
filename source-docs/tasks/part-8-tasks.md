# Tasks — Part 8: Data Standards (Cadence)

- **[x] T8.1 Canonical schema + classification** — Entities + PII/financial/secret classification. Priority: P0. Estimate: 2d. Dependencies: T5.2. DoD: schema migrated; fields classified. → `@cadence/db/drizzle/0000_init.sql` (canonical entities, idempotency keys, indexes) + `@cadence/core/data-classification.ts` taxonomy. Consent ledger & suppression tables also created (supports T8.4).
- **[x] T8.2 Idempotent write-back** — External IDs + idempotency keys on all mutations. Priority: P0. Estimate: 1.5d. Dependencies: T8.1, T4.1. DoD: replays cause no double-apply. → idempotency keys on invoices/payments/usage/messages; `applyPayment` keys payments on (tenant,system,external_id) + connector `writeBackPayment` idempotency key. Replay→duplicate proven live on Neon.
- **[x] T8.3 Reconciliation + drift alerts** — Nightly balance reconcile vs accounting. Priority: P0. Estimate: 2d. Dependencies: T8.2. DoD: accuracy >99.9%; drift alerts fire. → `@cadence/domain` `reconcileInvoices` (compares local vs connector balances, emits `reconcile.drift` audit + Sentry alert); worker `reconcile` queue wired.
- **[x] T8.4 Consent ledger + suppression list** — Per-channel/contact consent + suppression store. Priority: P0. Estimate: 1.5d. Dependencies: T8.1. DoD: queried by send path. → `consent_records` + `suppression_entries` tables (T8.1) + `@cadence/security` `consent.ts` (`checkConsent`/`recordConsent`/`suppressContact`/`isSuppressed`/`latestConsent`), called by `sendMessage` before every dispatch.
- **[~] T8.5 Retention/erasure + backups** — Retention policy, erasure-on-request w/ legal hold, PITR + restore test. Priority: P1. Estimate: 1.5d. Dependencies: T8.1. DoD: erasure works; restore test passes. → `@cadence/domain` `eraseCustomer` (redacts PII, retains financial records under legal hold) + `POST /customers/:id/erase`; `RETENTION_POLICY` from data classification. **Remaining:** PITR backup config + restore drill (ops).
