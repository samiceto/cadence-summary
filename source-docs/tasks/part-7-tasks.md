# Tasks — Part 7: Security Requirements (Cadence)

- **[x] T7.1 RLS + RBAC** — Tenant RLS + roles (Owner/Finance/Viewer/Approver). Priority: P0. Estimate: 2d. Dependencies: T5.2. DoD: RLS tests pass; role checks enforced. → RLS (Part 5); api `TenantContextMiddleware` (Clerk `verifyToken` + membership/role lookup, partner multi-tenant via `x-cadence-tenant`), `RolesGuard` + `@Roles` on controllers.
- **[x] T7.2 Token vault + scopes** — Encrypted OAuth tokens, least-privilege, revocation handling. Priority: P0. Estimate: 2d. Dependencies: T4.1. DoD: tokens encrypted, never logged; revocation honored. → `@cadence/security` AES-256-GCM `crypto.ts` + `tokens.ts` (`saveConnectionTokens`/`getConnectionToken`/`revokeConnection`). Needs `CADENCE_ENCRYPTION_KEY`.
- **[x] T7.3 Consent + suppression gate** — Consent ledger + suppression check before every send. Priority: P0. Estimate: 2d. Dependencies: T4.3, T8.4. DoD: no send without consent; STOP suppresses immediately. → `@cadence/security` `consent.ts` (`checkConsent`/`suppressContact`) wired into the send path (`@cadence/domain` `sendMessage`).
- **[x] T7.4 Audit log + export** — Immutable log of actions/messages/approvals/write-backs. Priority: P0. Estimate: 1.5d. Dependencies: T5.2. DoD: all sensitive actions logged + exportable. → immutable `audit_events` (RLS, no UPDATE/DELETE) written across all services + `GET /audit/export` (owner/finance).
- **T7.5 Scanning + IR runbook** — Dependency/secret scanning; incident response doc. Priority: P1. Estimate: 1d. Dependencies: T5.1. DoD: CI scans enabled; runbook published.
