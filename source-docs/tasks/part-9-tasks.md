# Tasks — Part 9: Scalability Rules (Cadence)

- **[x] T9.1 Delta sync + webhooks** — Cursor-based incremental sync + inbound webhooks. Priority: P0. Estimate: 2d. Dependencies: T4.1. DoD: freshness <15 min; no full pulls. → signature-verified `POST /webhooks/stripe/:tenantId` (invoice + payment + subscription events → ingest/applyPayment) + `POST /webhooks/twilio/:tenantId` (inbound SMS → handleReply). Shared `@cadence/domain` `ingest.ts` upsert helpers. Nightly full sync remains the backstop.
- **[~] T9.2 Worker autoscaling + DLQ** — Scale by queue depth; dead-letter queue. Priority: P1. Estimate: 1.5d. Dependencies: T5.3. DoD: scales under load; failures to DLQ. → DLQ + exponential-backoff retries in `@cadence/queue`. **Remaining:** queue-depth autoscaling config (deploy-time).
- **[~] T9.3 Sending throttle + failover** — Per-tenant identities, warmup throttling, provider failover. Priority: P0. Estimate: 2d. Dependencies: T4.4. DoD: rate limits respected; failover works. → Redis token-bucket `throttle` (per-tenant/channel warmup caps) wired into send worker + `FailoverEmail/SmsAdapter`. **Remaining:** per-tenant sending identities (domain config).
- **[x] T9.4 AI batching + rate-limit** — Batch scoring; queue/rate-limit LLM calls. Priority: P1. Estimate: 1d. Dependencies: T6.1. DoD: cost/latency targets met. → batch `scoreOpenInvoices` + `RateLimitedLlmAdapter` (concurrency + min-interval) wrapping the worker LLM.
- **T9.5 Load test + indexing/archival** — 10× load test; indexes + archival. Priority: P1. Estimate: 1.5d. Dependencies: T8.1. DoD: targets hold at 10×; no loss/dup.
